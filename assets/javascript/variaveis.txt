variáveis

TELAS DE CRIACAO:

const telaDeCriacao = document.querySelectorAll('.etapa-de-criacao');
const telaDeInfosBasicas = telaDeCriacao[0];
const telaPerguntas = telaDeCriacao[1];
const telaNiveis = telaDeCriacao[2];
const telaSucesso = telaDeCriacao[3];

CAMPOS DE ENTRADA DE DADOS:

-> TELA DE INFOS BASICAS :

const caixaDadosBasicos = document.querySelector('.entrada-de-dados');
const titulo = caixaDadosBasicos.children[0].value;
const imagem = caixaDadosBasicos.children[1].value;
const totalDePerguntas = parseFloat(caixaDadosBasicos.children[2].value);
const totalDeNiveis = parseFloat(caixaDadosBasicos.children[3].value);

E vou precisar de const telaDeInfosBasicas
E vou precisar de const telaPerguntas.

-> TELA DE PERGUNTAS :

const containerPerguntas = document.querySelectorAll('.entrada-de-perguntas');

const caixaPerguntas =  document.querySelectorAll('.caixa-perguntas');
const pergunta = caixaPerguntas[i].children[0].value;
const cor = caixaPerguntas[i].children[1].value;

const caixaRespostaCorreta = document.querySelectorAll('.resposta-correta');
const respostaCorreta = caixaRespostaCorreta[i].children[0].value;
const imagemRespostaCorreta = caixaRespostaCorreta[i].children[1].value;

const caixaRespostaIncorreta = document.querySelectorAll('.respostas-incorretas');
const respostaIncorreta = caixaRespostaIncorreta[i].children[i+2].value;
const imagemRespostaCorreta = caixaRespostaIncorreta[i].children[(i+1)+2].value;

E vou precisar de const totalDePerguntas.
E vou precisar de const telaPerguntas.
E vou precisar de const telaNiveis.

-> TELA DE NÍVEIS :

const containerNiveis = document.querySelectorAll('.entrada-de-niveis');

const caixaNiveis =  document.querySelectorAll('.caixa-niveis');
const tituloNivel = caixaNiveis[i].children[0].value;
const porcentagemMinima = caixaNiveis[i].children[1].value;
const imagemNivel = caixaNiveis[i].children[2].value;
const descricaoNivel = caixaNiveis[i].children[3].value;

E vou precisar de const totalDeNiveis.
E vou precisar de const telaNiveis.
E vou precisar de const telaSucesso.

-> TELA SUCESSO :

